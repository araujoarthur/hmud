function(context, args)
{
	var caller = context.caller;
	var l = #fs.scripts.lib();
	let msg="`ACome to socialmud.com ``LBETA``A to interact with other players inside hackmud with user-friendly interface provided by ``Rsocial``Cmud``A!`"
	#fs.chats.send({channel:"0000", msg:msg});
}
