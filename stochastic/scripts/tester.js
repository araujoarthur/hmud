function(context, args)
{
	var caller = context.caller;
	var l = #fs.scripts.lib();
	let loc = #ns.unidentified_jrttl_jy79cy.entry_u4jh5e();
	if (loc == ":::TRUST COMMUNICATION::: hardline required - activate with kernel.hardline"){
		return {ok:true}
	}else{
		return {ok:false}
	}

}
